# Size and Extension Restriction Tests

The files in this directory are used for testing the size and file extension restrictions for firmware files:

* Size must be <= 200 kB
* Extension must be ".bin"

