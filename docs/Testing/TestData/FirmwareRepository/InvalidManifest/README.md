This directory contains firmware repository manifests that deliberately violate
the manifest contract.

These files are test fixtures for test cases.

Do not use these manifests as production firmware repository manifests.